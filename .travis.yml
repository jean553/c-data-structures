sudo: required
language: c
dist: xenial

before_script:
    - sudo apt-get update
    - sudo apt-get install gcc cmake check -y
script: 
    - cmake --version
    - mkdir build && cd build/
    - cmake ..
    - make
    - ctest --output-on-failure .
